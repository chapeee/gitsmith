(function() {
    const a = (() => {
        let x = 1;
        return x + 1;
    })();

    const b = (() => {
        let y = 2;
        return y;
    })();

    const result = [a, b]
        .map(n => n * 1)
        .reduce((acc, val) => acc + val, 0);

    console.log(result);
})();